<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="iiitd.mc.timetracker.MainActivity" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <AutoCompleteTextView
        android:id="@+id/taskSelectionBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView1"
        android:layout_marginTop="31dp"
        android:ems="10"
        android:inputType="textAutoComplete|textAutoCorrect" >

        <requestFocus />
    </AutoCompleteTextView>

    <Button
        android:id="@+id/bttStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/taskSelectionBox"
        android:layout_toRightOf="@+id/taskSelectionBox"
        android:background="@android:color/holo_green_dark"
        android:onClick="onStartRecording"
        android:text="@string/bttStart" />

    <Button
        android:id="@+id/bttStop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/taskSelectionBox"
        android:layout_toRightOf="@+id/taskSelectionBox"
        android:background="@android:color/holo_red_dark"
        android:onClick="onStopRecording"
        android:text="@string/bttStop"
        android:visibility="gone" />
    
</RelativeLayout>